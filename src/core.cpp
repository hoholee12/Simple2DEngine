class core2D{
public:
    void initCore2D(){
        //initialize engine
        //physics2D();
    }


    /*
    *   each object has a mesh model = physics = collision
    *   each object is saved in the list with id as index.(expects game to identify objects using the id)
    *   physics can be nonexistant(stay in set position, HUD?), or movable(physics2D init, and only movable using velocity and direction)
    *   turn off physics2D to reposition object
    */
    








};